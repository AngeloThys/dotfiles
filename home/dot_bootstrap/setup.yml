---
- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true
  tasks:
    - name: Lsd installation
      ansible.builtin.include_tasks:
        file: lsd.yml  
      tags:
      - f-lsd
    - name: Lazyvim setup
      ansible.builtin.include_tasks:
        file: lazyvim.yml
      tags:
      - f-lazyvim
